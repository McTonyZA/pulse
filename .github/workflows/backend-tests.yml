name: Backend Tests

on:
  push:
    branches:
      - main
      - release/*
    paths:
      - 'backend/server/**'
      - '.github/workflows/backend-tests.yml'
  pull_request:
    paths:
      - 'backend/server/**'
      - '.github/workflows/backend-tests.yml'
  workflow_dispatch:

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  test:
    name: Run Backend Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend/server

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('backend/server/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Run tests with coverage
        run: mvn clean verify

      - name: Publish test report
        if: always()
        uses: dorny/test-reporter@v1
        with:
          name: Backend Test Results
          path: backend/server/target/surefire-reports/*.xml
          reporter: java-junit
          fail-on-error: true

      - name: Code coverage summary
        if: success()
        run: |
          python3 << 'EOF'
          import xml.etree.ElementTree as ET
          import os
          import sys

          try:
              tree = ET.parse('target/site/jacoco/jacoco.xml')
              root = tree.getroot()

              counter = root.find("./counter[@type='LINE']")

              if counter is not None:
                  missed = int(counter.get('missed', 0))
                  covered = int(counter.get('covered', 0))
                  total = missed + covered

                  if total > 0:
                      percentage = (covered / total) * 100

                      with open(os.environ['GITHUB_STEP_SUMMARY'], 'a') as f:
                          f.write('### 📊 Code Coverage Report\n\n')
                          f.write('#### Overall Coverage\n')
                          f.write(f'**Coverage:** {percentage:.2f}%\n')
                          f.write(f'- Lines Covered: {covered}\n')
                          f.write(f'- Lines Missed: {missed}\n')
                          f.write(f'- Total Lines: {total}\n\n')

                          if percentage < 35:
                              f.write(f'❌ **Build Failed: Overall coverage ({percentage:.2f}%) is below minimum threshold of 35%**\n\n')
                              sys.exit(1)
                          else:
                              f.write('✅ Coverage meets minimum threshold of 35%\n\n')
                  else:
                      with open(os.environ['GITHUB_STEP_SUMMARY'], 'a') as f:
                          f.write('### 📊 Code Coverage Report\n\n')
                          f.write('⚠️ No coverage data available\n\n')
              else:
                  with open(os.environ['GITHUB_STEP_SUMMARY'], 'a') as f:
                      f.write('### 📊 Code Coverage Report\n\n')
                      f.write('⚠️ LINE counter not found in report\n\n')
          except Exception as e:
              with open(os.environ['GITHUB_STEP_SUMMARY'], 'a') as f:
                  f.write('### 📊 Code Coverage Report\n\n')
                  f.write(f'⚠️ Unable to parse coverage data: {str(e)}\n\n')
          EOF

      - name: Check overall coverage via JaCoCo XML
        if: success()
        working-directory: backend/server
        run: |
          python3 $GITHUB_WORKSPACE/.github/scripts/check_backend_coverage.py \
            --report target/site/jacoco/jacoco.xml \
            --min-line 35
      - name: Checkstyle summary
        if: always()
        run: |
          if [ -f target/checkstyle/checkstyle-report.xml ]; then
            violations=$(grep -c '<error' target/checkstyle/checkstyle-report.xml || echo "0")
            echo "### 🎨 Checkstyle Report" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            if [ "$violations" -eq "0" ]; then
              echo "✅ No Checkstyle violations found!" >> $GITHUB_STEP_SUMMARY
            else
              echo "⚠️ Found $violations Checkstyle violations" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "📄 Download the full report from the workflow artifacts." >> $GITHUB_STEP_SUMMARY
            fi
          fi
  required-status-check:
    permissions:
      contents: read
    needs:
      - test
    runs-on: ubuntu-latest
    if: always()
    steps:
      - if: needs.test.result != 'success'
        run: exit 1
